<?xml version="1.0" encoding="UTF-8"?>
<LightningComponentBundle xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>60.0</apiVersion>
    <isExposed>true</isExposed>
    <masterLabel>bt_RecordDuplicateManager</masterLabel>
    <targets>
        <target>lightning__RecordPage</target>
        <target>lightning__AppPage</target>
        <target>lightning__HomePage</target>
    </targets>
    <targetConfigs>
        <!-- Contact Record Pages -->
        <targetConfig targets="lightning__RecordPage">
            <objects>
                <object>Contact</object>
            </objects>
            <property name="fieldSetName"
                      type="String"
                      label="Contact Field Set API Name"
                      description="Controls which Contact fields show for each duplicate when viewed. Defaults to 'Duplicate_Review'."/>
            <property name="fieldSetMergeName"
                      type="String"
                      label="Contact Merging Field Set API Name"
                      description="Controls which Contact fields will be allowed to be merged via 'Quick Merge' functionality. Defaults to 'Duplicate_Review'."/>
            <property name="maxSets"
                      type="Integer"
                      label="Max Duplicate Record Sets to Load"
                      default="25"/>
        </targetConfig>

        <!-- App/Home Pages -->
        <targetConfig targets="lightning__AppPage,lightning__HomePage">
            <property name="fieldSetName"
                      type="String"
                      label="Contact Field Set API Name"/>
            <property name="fieldSetMergeName"
                      type="String"
                      label="Contact Merging Field Set API Name"/>
            <property name="maxSets"
                      type="Integer"
                      label="Max Duplicate Record Sets to Load"
                      default="25"/>
        </targetConfig>
    </targetConfigs>
</LightningComponentBundle>